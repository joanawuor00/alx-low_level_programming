#include <stdio.h>
/**
 * main- a program that prints a line of text using printf function
 *
 * Return: 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
	Return(0);
}
